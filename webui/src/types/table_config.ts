export interface ITableConfigDataType {
    title: string;
    key: string;
    selected: boolean;
}

export const columnDefine = [
    {
        title: '列名',
        dataIndex: 'title',
        key: 'title',
    },
];
